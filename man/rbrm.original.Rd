% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rbrm_original.r
\name{rbrm.original}
\alias{rbrm.original}
\title{Regularized Binary Regression Model (RBRM)}
\usage{
rbrm.original(
  va,
  vb,
  x,
  y,
  alpha.start = NULL,
  beta.start = NULL,
  max.step = 3000,
  thres = 1e-04,
  lambda = 0,
  lr.alpha = 0.06,
  lr.beta = 0.02,
  intercept = TRUE,
  early_stopping_rounds = 10
)
}
\arguments{
\item{va}{A matrix of independent variables (without an intercept) for alpha.}

\item{vb}{A matrix of independent variables (without an intercept) for beta. If NULL, va is used.}

\item{x}{A vector indicating the group assignment (1 or 0) for each observation.}

\item{y}{A vector of binary outcomes (0/1).}

\item{alpha.start}{Initial values for alpha coefficients. Defaults to a zero vector.}

\item{beta.start}{Initial values for beta coefficients. Defaults to a vector of 0.01.}

\item{max.step}{Maximum number of optimization steps. Default is 3000.}

\item{thres}{Threshold for convergence. Default is 1e-04.}

\item{lambda}{Regularization parameter for L1 penalty. Default is 0 (no regularization).}

\item{lr.alpha}{Learning rate for alpha parameters. Default is 0.06.}

\item{lr.beta}{Learning rate for beta parameters. Default is 0.02.}

\item{intercept}{Logical. If TRUE, the intercept is included and not penalized. Default is TRUE.}

\item{early_stopping_rounds}{The number of rounds without improvement before early stopping occurs.}
}
\value{
A list with the following components:
\item{point.est}{The estimated alpha and beta coefficients.}
\item{convergence}{Logical indicating whether convergence was achieved within the max steps.}
\item{value}{The penalized negative log-likelihood value at convergence.}
\item{step}{The number of steps taken to converge.}
}
\description{
Performs a regularized binary regression model (RBRM) using FISTA proximal gradient descent.
The model penalizes the negative log-likelihood and includes optional early stopping.
}
\examples{
# Example usage:
#va <- matrix(c(1, 1, 1, 1, 0, 0, 0, 0), ncol = 2)
#vb <- matrix(c(1, 1, 0, 0, 1, 1, 0, 0), ncol = 2)
#y <- c(1, 0, 1, 0)
#x <- c(1, 1, 0, 0)
#result <- rbrm(va, vb, y, x)

}
