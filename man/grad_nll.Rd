% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradients.R
\name{grad_nll}
\alias{grad_nll}
\title{Calculate Analytical Gradient of User's nllh Function (RR Model) - Refined Check
Calls the stable derivative helper function.}
\usage{
grad_nll(
  alpha,
  beta,
  y,
  x,
  va,
  vb,
  prob_fun,
  opt = c("both", "alpha", "beta"),
  method = c("analytical", "numerical"),
  clipping = 1e-10
)
}
\arguments{
\item{alpha}{Vector of parameters for theta.}

\item{beta}{Vector of parameters for phi.}

\item{y}{Vector of binary outcomes (Y in paper).}

\item{x}{Vector of binary exposures/treatments (A in paper).}

\item{va}{Design matrix for alpha parameters (W in paper). Rows are observations.}

\item{vb}{Design matrix for beta parameters (Z in paper). Rows are observations.}

\item{prob_fun}{Function that takes theta (theta), phi (phi) and returns list(p0, p1).}

\item{opt}{Character string specifying which gradient to return ("alpha", "beta", or "both").}

\item{clipping}{Small epsilon for probability clipping (defaults to 1e-10).}
}
\value{
Analytical gradient vector(s) of the negative log-likelihood defined by user's nllh.
}
\description{
Calculate Analytical Gradient of User's nllh Function (RR Model) - Refined Check
Calls the stable derivative helper function.
}
